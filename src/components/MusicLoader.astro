<div class="container">
  <div class="plate">
    <div class="black">
      <div class="border">
        <div class="white">
          <div class="center"></div>
        </div>
      </div>
    </div>
  </div>

  <div class="player">
    <div class="rect"></div>
    <div class="circ"></div>
  </div>
</div>
<style>
:root {
  /* Colors */
  --color-bg: #c3ebd1;
  --color-dark: #675d50;
  --color-light: #f3deba;
  /* Size */
  --size-container: 50px;
  --size-plate: 45px;
  --size-plate-border: 34px;
  --size-plate-white: 23px;

  --size-circ: 10px;
  --width-rect: 3px;
  --height-rect: 16px;

  --size-circle-center: 10px;
  --size-box-shadow: 3px;
}
  .container {
    width: var(--size-container);
    height: var(--size-container);
    background-color: var(--color-bg);
    border-radius: 10px;
    position: relative;
    box-shadow: var(--size-box-shadow) var(--size-box-shadow) 0 0 var(--color-dark);
  }

  .plate {
    width: fit-content;
  }

  .plate .black,
  .plate .white,
  .plate .center,
  .plate .border {
    border-radius: 100%;
  }

  .container,
  .plate .black,
  .plate .white,
  .plate .border {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .plate .black {
    width: var(--size-plate);
    height: var(--size-plate);
    background-color: var(--color-dark);
    animation: rotation 2s infinite linear;
  }

  @keyframes rotation {
    from {
      transform: rotate(0deg);
    }

    to {
      transform: rotate(359deg);
    }
  }

  .plate .white {
    width: var(--size-plate-white);
    height: var(--size-plate-white);
    background-color: var(--color-light);
  }

  .plate .center {
    width: var(--size-circle-center);
    height: var(--size-circle-center);
    background-color: var(--color-dark);
  }

  .plate .border {
    width: var(--size-plate-border);
    height: var(--size-plate-border);
    border-top: 3px solid var(--color-light);
    border-bottom: 3px solid var(--color-light);
    border-left: 3px solid var(--color-dark);
    border-right: 3px solid var(--color-dark);
  }

  .player {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    width: fit-content;
    position: absolute;
    bottom: 0;
    right: 0;
    margin-bottom: 8px;
    margin-right: 8px;
    rotate: -45deg;
  }

  .player .circ {
    width: var(--size-circ);
    height: var(--size-circ);
    background-color: var(--color-light);
    border-radius: 100%;
    z-index: 1;
  }

  .player .rect {
    width: var(--width-rect);
    height: var(--height-rect);
    background-color: var(--color-light);
    position: absolute;
    bottom: 0;
    margin-bottom: 5px;
  }
</style>
