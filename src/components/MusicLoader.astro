<div class="container">
  <div class="plate">
    <div class="black">
      <div class="border">
        <div class="white">
          <div class="center"></div>
        </div>
      </div>
    </div>
  </div>

  <div class="player">
    <div class="rect"></div>
    <div class="circ"></div>
  </div>
</div>
<style>
:root {
  /* Colors */
  --color-bg: #c3ebd1;
  --color-dark: #675d50;
  --color-light: #f3deba;
  /* Size */
  --music-loader-scale: 1;

  --size-container: calc(50px * var(--music-loader-scale));
  --size-plate: calc(45px * var(--music-loader-scale));
  --size-plate-border: calc(34px * var(--music-loader-scale));
  --size-plate-white: calc(23px * var(--music-loader-scale));

  --size-circ: calc(10px * var(--music-loader-scale));
  --width-rect: calc(3px * var(--music-loader-scale));
  --height-rect: calc(16px * var(--music-loader-scale));

  --size-circle-center: calc(10px * var(--music-loader-scale));
  --size-box-shadow: calc(3px * var(--music-loader-scale));
}
  .container {
    width: var(--size-container);
    height: var(--size-container);
    background-color: var(--color-bg);
    border-radius: 10px;
    position: relative;
    box-shadow: var(--size-box-shadow) var(--size-box-shadow) 0 0 var(--color-dark);
  }

  .plate {
    width: fit-content;
  }

  .plate .black,
  .plate .white,
  .plate .center,
  .plate .border {
    border-radius: 100%;
  }

  .container,
  .plate .black,
  .plate .white,
  .plate .border {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .plate .black {
    width: var(--size-plate);
    height: var(--size-plate);
    background-color: var(--color-dark);
    animation: rotation 2s infinite linear;
  }

  @keyframes rotation {
    from {
      transform: rotate(0deg);
    }

    to {
      transform: rotate(359deg);
    }
  }

  .plate .white {
    width: var(--size-plate-white);
    height: var(--size-plate-white);
    background-color: var(--color-light);
  }

  .plate .center {
    width: var(--size-circle-center);
    height: var(--size-circle-center);
    background-color: var(--color-dark);
  }

  .plate .border {
    width: var(--size-plate-border);
    height: var(--size-plate-border);
    border-top: 3px solid var(--color-light);
    border-bottom: 3px solid var(--color-light);
    border-left: 3px solid var(--color-dark);
    border-right: 3px solid var(--color-dark);
  }

  .player {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    width: fit-content;
    position: absolute;
    bottom: 0;
    right: 0;
    margin-bottom: 8px;
    margin-right: 8px;
    rotate: -45deg;
  }

  .player .circ {
    width: var(--size-circ);
    height: var(--size-circ);
    background-color: var(--color-light);
    border-radius: 100%;
    z-index: 1;
  }

  .player .rect {
    width: var(--width-rect);
    height: var(--height-rect);
    background-color: var(--color-light);
    position: absolute;
    bottom: 0;
    margin-bottom: 5px;
  }
</style>
